appId: com.coffstack.digitalnomadapp
---
- launchApp:
    clearState: true

- runFlow: sign-in.yml

# Sign out
- tapOn:
    text: .*Perfil.*

- repeat:
    while:
      notVisible: ".*Bem-vindo.*"
    commands:
      - tapOn:
          id: "sign-out-button"

- assertVisible: ".*Bem-vindo.*"
