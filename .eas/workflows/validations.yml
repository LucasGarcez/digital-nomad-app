name: Validation

on:
  pull_request:
    branches: ["main"]

jobs:
  code-validation:
    steps:
      - uses: eas/checkout
      - uses: eas/install_node_modules

      - name: Run TypeScript compiler
        run: npx tsc --noEmit
      - name: Run Tests
        run: npm run test:ci
